# 云端部署方案与步骤

> 面向：course_local 本地 Flask 课程管理系统（SQLite）。
> 项目极轻（仅依赖 Flask），已备好部署三件套：`requirements.txt` / `Procfile` / `Dockerfile`。
> app.py 已支持 `PORT` 环境变量、host 默认 `0.0.0.0`，无需改代码即可上云。

---

## 一、成本与选型（先看这条，避免被"烧积分"）

你之前被腾讯云"按调用量扣积分"坑过（2 天烧 3000 分 → SERVICE_FORBIDDEN）。
**核心结论：选"按固定费用计费"的部署方式，而不是"按调用量/积分"的。**

| 方案 | 计费方式 | 是否会被"烧" | 月成本 | 适合 |
|---|---|---|---|---|
| **轻量云服务器**（腾讯云/阿里云轻量） | 固定月/年费 | ❌ 访问再多不加钱 | 学生认证约 ¥5-8/月（年付更低） | ✅ 推荐，长期稳定用 |
| **免费 PaaS**（Render / Railway / Zeabur） | 免费额度 | 免费档有小时数/冷启动限制 | ¥0（但数据在临时盘，易丢） | 临时验证功能 |
| 云函数/Serverless（CFC/SCF/FaaS） | **按调用量** | ⚠️ 与你踩过的坑同类型 | 波动 | ❌ 不推荐 |
| 内网穿透（cpolar/花生壳） | 免费/低 | 不算云端，依赖本机开机 | ¥0 | 电脑必须常开 |

**推荐：腾讯云/阿里云「轻量应用服务器」**——固定价格，访问多少次都不额外收费，天然规避"积分烧尽"问题。SQLite 数据存在服务器磁盘，长期稳定。

---

## 二、部署步骤（以轻量云服务器 + Docker 为例）

### 1. 买服务器
- 腾讯云 / 阿里云「轻量应用服务器」，选 **Ubuntu 22.04 或 Debian 12** 镜像，2核2G 档即可（本应用极轻）。
- 学生认证后价格很低（约 ¥60-90/年）。
- 开放端口：**80（HTTP）**，可选 443（HTTPS）。

### 2. 上传代码
```bash
# 本地（Windows PowerShell），用 scp 上传整个 course_local 目录到服务器
scp -r course_local root@服务器IP:/opt/course
```

### 3. 服务器上安装 Docker 并启动
```bash
ssh root@服务器IP
apt update && apt install -y docker.io
cd /opt/course
docker build -t course-app .
# 用 volume 挂载 data，数据持久化、升级不丢
docker run -d --name course \
  -p 80:7860 \
  -v /opt/course/data:/app/data \
  -e SECRET_KEY='换成你的随机密钥' \
  --restart always \
  course-app
```

### 4. 访问
- 手机/电脑访问 `http://服务器IP/` 即可。
- 配置域名 + HTTPS（可选）：用宝塔面板或 Caddy 一行搞定。

### 5. 数据更新（重要，与本地一致）
- 服务器上的 `course.db` 在 `/opt/course/data/`（volume）。
- 以后改数据：本地改 → 用 `sync_full.py` 同步本地副本 → 把 `data/course.db` 单独上传覆盖服务器上的（先停容器再覆盖，避免写坏）。
- 或直接在服务器页面上录入（前端有完整录入界面）。

---

## 三、免费 PaaS 测试（0 成本，临时验证用）

### Koyeb（当前在试：免费、无需信用卡、永久免费档）
1. 代码已推 GitHub：`https://github.com/pang996/river`（Dockerfile 已适配：优先读平台注入的 `PORT`，默认 7860）。
2. 注册 https://app.koyeb.com（邮箱即可，**无需绑卡**）→ Create App → 连接 GitHub → 选 `river` 仓库、`main` 分支。
3. 配置：
   - Builder: Dockerfile（自动检测）
   - Instance Type: **Free（Nano）**
   - Region: Frankfurt / Washington（免费档仅这两个）
   - Environment Variables: `SECRET_KEY` = 一串随机字符
4. Deploy → 等 1-3 分钟 → Healthy。
5. 访问地址形如 `https://xxx-你的账号.koyeb.app`，登录页账号 `admin` / `admin123`。
6. 免费档限制：闲置休眠（首次访问等 1-2s 唤醒）、磁盘不持久（重启丢数据）→ **仅测试用**。

### Render（https://render.com，需绑卡验证）
1. 把 course_local 推到 GitHub 仓库。
2. Render → New Web Service → 选仓库。
3. Build Command: `pip install -r requirements.txt`；Start Command: `gunicorn -w 1 -b 0.0.0.0:$PORT app:app`
4. 免费档：每月 750 小时、无域名 HTTPS、冷启动慢、**磁盘临时**（重启丢数据，仅适合功能测试）。绑卡只验证不扣费（可能有 $1 预授权退回）。

### Railway / Zeabur
- 用法类似，直接选 Dockerfile 即可。
- 同样免费档磁盘不持久，仅测试用。

---

## 三·五、登录账号配置（v1.14 起，部署必做）

| 项 | 值 |
|---|---|
| 默认账号 | `admin` / 密码 `admin123` |
| 会话 | 12 小时，Cookie 名 `course_sid` |
| 环境变量 `SECRET_KEY` | **必须设置**（随机字符串）；不设则用本地默认 `dev-secret-change-me-in-prod`，有安全风险 |
| 登录后 | **立即在右上角"修改密码"改掉默认密码** |

密码以 werkzeug 哈希存于 SQLite `users` 表，前端未登录访问 /api/* 返回 401，自动弹登录框。

---

## 四、部署前本地自检（已确认）

- [x] 依赖仅 Flask（requirements.txt 已生成）
- [x] app.py 支持 `PORT` 环境变量（PaaS 约定），host 默认 0.0.0.0
- [x] Dockerfile / Procfile / .dockerignore 已就绪（Dockerfile 监听 ${PORT:-7860}）
- [x] SQLite 单用户读写，gunicorn 1 worker 最稳（已配置）
- [x] 账号登录验证 v1.14 本地全链路测试通过（登录/登出/改密）
- [x] 代码已推送 GitHub：`https://github.com/pang996/river`（HEAD 47dc1e6）
- [ ] 待确认：Koyeb 部署是否成功（用户操作中）
